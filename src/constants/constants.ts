export enum Size {
  Small = 'sm',
  Medium = 'md',
  Large = 'lg',
}

export enum BaseButtonType {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum LoadingMaskType {
  Solid = 'solid',
  Blur = 'blur',
}
